# dedicated-terminal-integration

I want a dedicated terminal next to the openspec window where openspec can send skill commands and control the terminal such that when specs are defined, the implementation is just one click away
